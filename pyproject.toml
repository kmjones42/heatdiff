[project]
name = "heatdiff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9.6",
]
